# Copia este archivo como `.env.local` y rellena con los valores del proyecto
# Supabase (Dashboard → Project Settings → API Keys). La key publishable/anon
# es pública por diseño (la autorización real la hace RLS); la service_role
# NUNCA va aquí ni al front.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
